# Broker API Credentials for Integration Tests
# Copy this file to .env and fill in your credentials
#
# IMPORTANT: Never commit .env to version control!

# Your Matba Rofex / Primary broker username
BROKER_USERNAME=your_username_here

# Your Matba Rofex / Primary broker password  
BROKER_PASSWORD=your_password_here

# API Base URL (optional - overrides BROKER_ENVIRONMENT if set)
# Examples:
#   https://api.remarkets.primary.com.ar  (reMarkets/demo)
#   https://api.primary.com.ar            (production)
BROKER_API_URL=

# Environment: 'reMarkets' (test/demo) or 'production' (live trading)
# Only used if BROKER_API_URL is not set
BROKER_ENVIRONMENT=reMarkets
